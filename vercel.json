{
  "version": 2,
  "buildCommand": "cd src/frontend && npm install && CI=false npm run build",
  "outputDirectory": "src/frontend/build",
  "devCommand": "cd src/frontend && npm start",
  "installCommand": "cd src/frontend && npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}